{
  "name": "nextdoor-clone",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/customer",
    "apps/admin",
    "packages/shared",
    "backend"
  ],
  "scripts": {
    "customer": "cd apps/customer && npx expo start",
    "admin": "cd apps/admin && npx expo start",
    "backend": "cd backend && npm run dev",
    "build:customer:android": "cd apps/customer && eas build --platform android --profile production",
    "build:customer:ios": "cd apps/customer && eas build --platform ios --profile production",
    "build:admin:android": "cd apps/admin && eas build --platform android --profile production",
    "build:admin:ios": "cd apps/admin && eas build --platform ios --profile production",
    "build:all:android": "npm run build:customer:android && npm run build:admin:android",
    "version:patch": "node scripts/version-bump.js patch",
    "version:minor": "node scripts/version-bump.js minor",
    "version:major": "node scripts/version-bump.js major"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
